"use client";

import LogoIcon from "@/icons/LogoIcon";
import { Box } from "@chakra-ui/react";
import { FC } from "react";

const Logo: FC = () => {
  return (
    <Box>
      <LogoIcon />
    </Box>
  );
};

export default Logo;
